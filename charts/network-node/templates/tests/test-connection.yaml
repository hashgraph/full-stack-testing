apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "network-node.fullname" . }}-test-connection"
  labels:
    {{- include "network-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "network-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
